const { Client } = require('pg'); const client = new Client({ connectionString: 'postgres://postgres:postgres@localhost:5432/ehealth_db' }); client.connect().then(() => client.query('SELECT * FROM user_branch_dept ORDER BY created_at DESC LIMIT 5').then(res => console.log(user_branch_dept:, res.rows)).then(() => client.query('SELECT * FROM doctors ORDER BY created_at DESC LIMIT 5').then(res => console.log(doctors:, res.rows))).finally(() => client.end()));
